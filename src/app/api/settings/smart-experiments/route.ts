import { NextResponse } from "next/server"; import { experimentSettingsSchema } from "@/lib/experiments/schemas"; import { getExperimentSettings, updateExperimentSettings } from "@/lib/experiments/service"; import { experimentApiError, experimentUnauthorized, experimentUserId } from "@/lib/experiments/api";
export async function GET() { const userId = experimentUserId(); if (!userId) return experimentUnauthorized(); try { return NextResponse.json(await getExperimentSettings(userId)); } catch (error) { return experimentApiError(error); } }
export async function PATCH(request: Request) { const userId = experimentUserId(); if (!userId) return experimentUnauthorized(); try { return NextResponse.json(await updateExperimentSettings(userId, experimentSettingsSchema.parse(await request.json()))); } catch (error) { return experimentApiError(error); } }
