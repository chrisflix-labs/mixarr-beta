import { NextResponse } from "next/server"; import { coverageApiError, coverageSession, coverageUnauthorized, numberParam } from "@/lib/libraryCoverageApi"; import { getCoverageTracks } from "@/lib/libraryCoverage";
export async function GET(request: Request) { const userId=coverageSession(); if(!userId)return coverageUnauthorized(); try{const p=new URL(request.url).searchParams; return NextResponse.json({data:await getCoverageTracks(userId,{libraryId:p.get("libraryId"),view:"overused",search:p.get("search")||undefined,page:numberParam(p,"page",1),pageSize:numberParam(p,"pageSize",50),sort:"overuse"})});}catch(error){return coverageApiError(error);} }
