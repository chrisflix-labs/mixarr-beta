import { NextResponse } from "next/server"; import { troubleshootingApiError, troubleshootingUserId } from "@/lib/troubleshooting/api"; import { previewDiagnosticBundle } from "@/lib/troubleshooting/service";
export async function POST(_: Request, { params }: { params: { sessionId: string } }) { try { return NextResponse.json(await previewDiagnosticBundle(troubleshootingUserId(), params.sessionId)); } catch (error) { return troubleshootingApiError(error); } }
