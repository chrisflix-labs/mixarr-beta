import { NextResponse } from "next/server"; import { troubleshootingApiError, troubleshootingUserId } from "@/lib/troubleshooting/api"; import { exportTroubleshootingSession } from "@/lib/troubleshooting/service";
export async function GET(_: Request, { params }: { params: { sessionId: string } }) { try { const value = await exportTroubleshootingSession(troubleshootingUserId(), params.sessionId); return new NextResponse(value.content, { headers: { "Content-Type": value.contentType, "Content-Disposition": `attachment; filename="${value.filename}"`, "Cache-Control": "no-store" } }); } catch (error) { return troubleshootingApiError(error); } }
