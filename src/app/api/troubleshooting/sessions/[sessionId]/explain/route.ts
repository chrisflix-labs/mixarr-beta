import { NextResponse } from "next/server"; import { troubleshootingApiError, troubleshootingUserId } from "@/lib/troubleshooting/api"; import { explainTroubleshootingSession } from "@/lib/troubleshooting/service";
export async function POST(request: Request, { params }: { params: { sessionId: string } }) { try { const body = await request.json().catch(() => ({})); return NextResponse.json({ session: await explainTroubleshootingSession(troubleshootingUserId(), params.sessionId, body.externalConfirmation === true) }); } catch (error) { return troubleshootingApiError(error); } }
