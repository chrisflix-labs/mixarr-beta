import { NextResponse } from "next/server";
import prisma from "@/lib/prisma";
import { orchestrationApiError, orchestrationSession, orchestrationUnauthorized, pageInput } from "@/lib/orchestration/api";
export async function GET(request: Request) { const userId = orchestrationSession(); if (!userId) return orchestrationUnauthorized(); const { page, pageSize, skip } = pageInput(request); try { const where = { userId, status: { in: ["QUEUED", "WAITING", "BLOCKED"] as any[] } }; const [items, total] = await Promise.all([prisma.playlistOrchestrationJob.findMany({ where, orderBy: { scheduledFor: "asc" }, skip, take: pageSize, include: { managedPlaylist: { select: { id: true, displayName: true, automationEnabled: true, lastCompletedAt: true, lastFailedAt: true } } } }), prisma.playlistOrchestrationJob.count({ where })]); return NextResponse.json({ items, pagination: { page, pageSize, total, pages: Math.ceil(total / pageSize) } }); } catch (error) { return orchestrationApiError(error); } }
