import { NextResponse } from "next/server";
import prisma from "@/lib/prisma";
import { aiRouteError, requireAiAdmin } from "@/ai/services/api";
export const dynamic = "force-dynamic";
export async function GET(request: Request) { try { await requireAiAdmin(); const url = new URL(request.url); const take = Math.min(100, Math.max(1, Number(url.searchParams.get("take") || 50))); const cursor = url.searchParams.get("cursor"); const rows = await prisma.aiRequestAudit.findMany({ take: take + 1, ...(cursor ? { cursor: { id: cursor }, skip: 1 } : {}), orderBy: [{ createdAt: "desc" }, { id: "desc" }], select: { id: true, requestId: true, correlationId: true, featureKey: true, providerConfigId: true, providerType: true, providerDisplayName: true, model: true, status: true, startedAt: true, completedAt: true, latencyMs: true, retryCount: true, streamingUsed: true, cancellationStatus: true, inputTokenCount: true, outputTokenCount: true, totalTokenCount: true, estimatedCost: true, responseByteCount: true, errorCategory: true, sanitizedErrorCode: true, createdAt: true } }); const hasMore = rows.length > take; return NextResponse.json({ records: rows.slice(0, take), nextCursor: hasMore ? rows[take - 1].id : null }); } catch (error) { return aiRouteError(error); } }
